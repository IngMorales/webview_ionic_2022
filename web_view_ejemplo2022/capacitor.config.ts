import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'web_view_ejemplo2022',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
